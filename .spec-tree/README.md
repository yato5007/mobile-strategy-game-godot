# .spec-tree

Recursive Spec Kit nodes live here after root Spec Kit defines the tree.

Bootstrap intentionally does not create game-design nodes because `MASTER_PROJECT_PLAN.md` requires Spec Kit to derive the game concept, systems, and branches.

Every future node must include the full artifact set listed in `SPEC_TREE_RULES.md`.
