# constitution.md — Reveal and Resolver Handoff

Non-negotiable: Reveal is the state where locked actions are sent to the resolver. Resolver must be deterministic. Reveal beat structure consumes timing from envelope. No implementation.
