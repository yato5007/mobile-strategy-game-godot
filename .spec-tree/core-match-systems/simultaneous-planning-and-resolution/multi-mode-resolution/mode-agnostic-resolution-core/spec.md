# spec.md — Mode-Agnostic Resolution Core
Shared resolution logic: intensity comparison, commit-time tie-break, claim-delta computation. Mode flag (FFA/2v2) enables mode-specific constraint layer. Core must not duplicate mode rules. No implementation.
